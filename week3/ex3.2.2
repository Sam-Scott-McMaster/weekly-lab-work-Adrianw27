a. 
The script executes the first argument passed to the script
Command: ./di "ls -l"

b. 
Prediction:a 1 a1C 10
Actual output: a 1 a1C 1*10
The only discrepancy was that I predicted $B*10 would carry out the operation 1*10, 
but since B is a string it just concatenated the two strings.


